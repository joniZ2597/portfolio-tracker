# ✅ Fixes "Build image no longer supported" on Netlify
# Forces Ubuntu Jammy 22.04 (replaces deprecated Focal 20.04)

[build]
  publish = "dist"          # ⚠️ Change to "build" if using Create React App
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION  = "10"
